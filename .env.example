# =============================================================================
# Surqo — Variables de entorno
# Copia este archivo como .env y rellena los valores reales.
# NUNCA commiteés el .env con valores reales.
# =============================================================================

# -----------------------------------------------------------------------------
# Anthropic / LLM
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-...          # API key de Anthropic (console.anthropic.com)
LLM_MODEL=claude-haiku-4-5-20251001  # Modelo a usar (no cambiar sin revisar costos)
LLM_MAX_TOKENS=1024

# -----------------------------------------------------------------------------
# Base de datos — Supabase
# -----------------------------------------------------------------------------
SUPABASE_URL=https://<proyecto>.supabase.co
SUPABASE_KEY=<anon-or-service-role-key>
DATABASE_URL=postgresql+asyncpg://<user>:<password>@<host>:5432/postgres

# -----------------------------------------------------------------------------
# Cache — Upstash Redis
# -----------------------------------------------------------------------------
REDIS_URL=rediss://default:<password>@<host>.upstash.io:6379
CACHE_TTL_CLIMATE=3600   # segundos
CACHE_TTL_ANALYSIS=3600

# -----------------------------------------------------------------------------
# MQTT — HiveMQ Cloud
# -----------------------------------------------------------------------------
HIVEMQ_HOST=<cluster>.s2.eu.hivemq.cloud
HIVEMQ_PORT=8883
HIVEMQ_USERNAME=<usuario-hivemq>
HIVEMQ_PASSWORD=<password-hivemq>
MQTT_TOPIC_PREFIX=surqo

# -----------------------------------------------------------------------------
# Email — Resend
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_...                 # API key de resend.com
FROM_EMAIL=alertas@surqo.io          # Dominio verificado en Resend

# -----------------------------------------------------------------------------
# Observabilidad — Logfire
# -----------------------------------------------------------------------------
LOGFIRE_TOKEN=<token-de-logfire>      # pydantic.dev/logfire

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
APP_ENV=development                   # development | production
CORS_ORIGINS=["http://localhost:3000","https://surqo.vercel.app"]

# =============================================================================
# GitHub Secrets requeridos para CI/CD (.github/workflows/ci-cd.yml)
# Configúralos en: Settings → Secrets and variables → Actions
# =============================================================================
#
# Secret name               | Descripción
# --------------------------+--------------------------------------------------
# ANTHROPIC_API_KEY         | Mismo que arriba — usado en tests
# SUPABASE_URL              | URL de tu proyecto Supabase
# SUPABASE_KEY              | Service Role Key (no la anon key en producción)
# RESEND_API_KEY            | API key de Resend para emails
# LOGFIRE_TOKEN             | Token de Logfire para observabilidad
# RENDER_DEPLOY_HOOK        | URL del deploy hook de Render (Settings → Deploy hooks)
# VERCEL_TOKEN              | Token de Vercel (Settings → Tokens)
# VERCEL_ORG_ID             | ID de tu organización en Vercel
# VERCEL_PROJECT_ID         | ID del proyecto Surqo en Vercel
# =============================================================================
